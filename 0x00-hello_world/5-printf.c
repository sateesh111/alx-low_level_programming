#include <stdio.h>

/**
 * main - Entry point
 *
 * printf: prints a string
 *
 * Return: Always 0 (success)
 */
int main(void)
{
  char My_msg[] = "with proper grammer, but the outcome is a piece of art,";

  printf("%s\n", My_msg);
  return (0);
}
